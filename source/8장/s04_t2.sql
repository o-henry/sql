-- t2
SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
START TRANSACTION;
USE madang;
SET SQL_SAFE_UPDATES=0; 
UPDATE Users 
SET     age=21
WHERE  id=1;

COMMIT;

SELECT * 
FROM   Users
WHERE  id=1;

-- > t1